fun main(): void = ({
    x = 2;
    y = 2;

    if x == 2 then
        x = x + 4 + y;
    else
        x = x - 4 - y;
    end;

    putInt(x);
}
where
    var x: int;
);

var y: int;

fun putInt(i: int): void = none;