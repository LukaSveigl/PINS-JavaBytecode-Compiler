fun main(): void = ({
    x = 1;
    y = 2;
    z = 3;

    if x < y & y < z & x < z then
        putChar('y');
    else
        putChar('n');
    end;
}
where
    var x: int;
    var y: int;
    var z: int;
);

fun putChar(c: char): void = none;